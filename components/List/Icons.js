import React from 'react';
import { Icon } from '@ui-kitten/components';

export const ArrowSideIcon = (props) => (
    <Icon name='arrow-ios-forward' fill='#fff' {...props} />
);

export const ArrowDownIcon = (props) => (
    <Icon name='arrow-ios-downward' fill='#fff' {...props} />
);

export const DeleteIcon = (props) => (
    <Icon name='trash-2' fill='#fff' {...props} />
);

export const EditIcon = (props) => (
    <Icon name='edit-2' fill='#fff' {...props} />
);

export const PinOffIcon = (props) => (
    <Icon name='pin-off' fill='#fff' pack='materialCommunity' {...props} />
);

export const PinIcon = (props) => (
    <Icon name='pin' fill='#fff' pack='materialCommunity' {...props} />
);